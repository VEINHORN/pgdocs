# Using `pgdocs` with Docker

## Build

From `pgdocs` folder type below command:

```shell
docker build -t pgdocs:latest .
```

## Run

From `pgdocs` directory run command:

```shell
docker run -it pgdocs:latest
```

